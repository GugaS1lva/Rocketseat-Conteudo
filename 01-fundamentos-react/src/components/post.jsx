import React from "react"

export const Post = () => {
  return (
    <p>Primeiro Post</p>
  )
}

{/* // Named Export vs Default Export:
named export … [ Importação feita usando chaves e Preserva o nome da função ]
usado em COMPONENTES.

default export … [ A nomeação é feita na importação ]
usado em NÃO COMPONENTES.
*/}
